{
  "name": "kvs.io",
  "version": "0.0.1",
  "description": "kvs.io initial go",
  "main": "api.ls",
  "scripts": {
    "start": "nodemon -L -e ls --exec lsc api.ls",
    "test": "mocha --reporter spec --compilers ls:LiveScript --growl test",
    "testmon": "STUBRIAK=true nodemon -L -e ls node_modules/mocha/bin/mocha --reporter spec --compilers ls:LiveScript --growl test"
  },
  "author": "Doug Luce",
  "license": "ISC",
  "dependencies": {
    "basho-riak-client": "^1.0.1",
    "ipware": "0.0.4",
    "restify": "^3.0.2"
  },
  "devDependencies": {
    "LiveScript": "^1.3.1",
    "async": "^0.9.0",
    "chai": "^2.2.0",
    "mocha": "^2.2.1",
    "nodemon": "1.3.0-6",
    "sinon": "^1.14.1"
  }
}
