{
  "name": "kvs.io",
  "version": "0.0.1",
  "description": "kvs.io initial go",
  "main": "api.ls",
  "scripts": {
    "start": "NODE_ENV=production lsc api.ls",
    "test": "NODE_ENV=test mocha --reporter spec --compilers ls:LiveScript --growl test/test-*ls",
    "startdev": "NODE_ENV=development nodemon -L -e ls --exec lsc api.ls",
    "testmon": "NODE_ENV=development nodemon -L -e ls node_modules/mocha/bin/mocha --reporter spec --compilers ls:LiveScript --growl test/test-*ls"
  },
  "author": "Doug Luce",
  "license": "ISC",
  "dependencies": {
    "basho-riak-client": "^1.0.1",
    "bunyan": "^1.3.5",
    "dtrace-provider": "^0.4.0",
    "ipware": "0.0.4",
    "restify": "^3.0.2"
  },
  "devDependencies": {
    "LiveScript": "^1.3.1",
    "async": "^0.9.0",
    "bunyan-prettystream": "^0.1.3",
    "chai": "^2.2.0",
    "mocha": "^2.2.1",
    "nodemon": "1.3.0-6",
    "sinon": "^1.14.1"
  }
}
